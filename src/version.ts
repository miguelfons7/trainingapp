// The app version is sourced from package.json at build time (injected by Vite
// as __APP_VERSION__ — see vite.config.ts and globals.d.ts). Do NOT hand-edit a
// literal here; bump package.json instead (e.g. `npm version patch`).
export const APP_VERSION = __APP_VERSION__
