// Injected at build time by Vite's `define` from package.json's `version`
// (see vite.config.ts). package.json is the single source of truth for the app
// version; do not hard-code a version literal anywhere in src.
declare const __APP_VERSION__: string
